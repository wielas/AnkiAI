[variables]
NIXPACKS_POETRY_VERSION = "2.1.0"

[phases.install]
cmds = [
    "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install poetry==$NIXPACKS_POETRY_VERSION && poetry install --without dev --no-interaction --no-ansi"
]
